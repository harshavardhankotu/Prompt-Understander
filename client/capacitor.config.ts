import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.omnibid.india",
  appName: "OmniBid India",
  webDir: "dist/public",
};

export default config;
